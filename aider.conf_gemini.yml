# Model Configuration
model: gemini-pro
weak_model: gemini-pro
edit_format: diff

# UI Settings
dark_mode: true

# Git Settings
git_auto_commit: false

# Editor Settings
# Uses the default editor from the environment

# Behavior Settings
auto_architect: false
